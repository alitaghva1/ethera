node_modules/
dist/
# PixelLab API key (+ any other local secrets). Generate your own at
# https://pixellab.ai/account and drop it into .env — never commit.
.env
.env.local
# PixelLab generation artifacts — intermediate raw outputs. Final
# assets live under public/assets/characters/ and ARE committed.
scripts/pixellab/out/
# PixelLab UI character-creator exports — the user exports a ZIP, we
# extract into imports/<name>/ with rotations + animations + metadata.
# Tens of megabytes of per-frame PNGs; regenerable on demand.
scripts/pixellab/imports/
