[gd_scene load_steps=2 format=3 uid="uid://b3wraithphasein01"]

; Wraith phase-in shimmer — spawned at the wraith's reappear point when
; it finishes a teleport-flank. Script-driven (see wraith_phase_in.gd)
; so the ring + sparks are built procedurally and self-free when the
; tween chain completes. Same convention the hit_spark / heal_pulse
; effects use.

[ext_resource type="Script" path="res://scripts/wraith_phase_in.gd" id="1_script"]

[node name="WraithPhaseIn" type="Node2D"]
script = ExtResource("1_script")
