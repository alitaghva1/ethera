[gd_scene load_steps=2 format=3 uid="uid://b3lorestone001"]

[ext_resource type="Script" path="res://scripts/lore_stone.gd" id="1_script"]

; Iter 38 lore stone. Optional secret-content interactable. Thin .tscn
; wrapper — all visuals + collision are code-built in lore_stone.gd
; so a new stone visual variant (different color, different glyph)
; stays a single-file edit on the script.

[node name="LoreStone" type="Area2D"]
collision_layer = 0
collision_mask = 2
monitorable = false
script = ExtResource("1_script")
